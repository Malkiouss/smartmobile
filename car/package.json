{
  "name": "smartmobile",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client run build && node scripts/copy-client-dist.js",
    "dev": "npm --prefix client run dev",
    "server": "npm --prefix server start",
    "server:dev": "npm --prefix server run dev"
  }
}
